{
  "devDependencies": {
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "axios": "^0.22.0"
  },
  "scripts": {
    "start:db": "json-server -w db.json",
    "start:parcel": "parcel index.html"
  }
}
